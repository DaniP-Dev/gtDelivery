import 'package:flutter/material.dart';

class EnEsperaPage extends StatelessWidget {
  const EnEsperaPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Vista de En Espera', style: TextStyle(fontSize: 22, fontWeight: FontWeight.bold)),
    );
  }
}
